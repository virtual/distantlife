{
  "name": "distantlife-frontend",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "sass": "^1.79.0"
  },
  "scripts": {
    "build:css": "sass source/sass/styles.scss static/styles.css --no-source-map --style=compressed",
    "watch:css": "sass --watch source/sass/styles.scss:static/styles.css --style=expanded"
  }
}
